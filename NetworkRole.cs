namespace Chess
{
    public enum NetworkRole
    {
        None,       // Not a network game
        Host,       // This player is hosting the game
        Client      // This player is connecting to a host
    }
} 